name: conciergex
description: conciergex web app 

environment:
  sdk: '^3.6.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.0
  flutter_dotenv: ^5.1.0
  intl: ^0.20.1
  dart_openai: ^5.0.0
  geolocator: ^13.0.2
  mapbox_maps_flutter: ^2.5.0
  google_maps_flutter: ^2.5.3
  google_maps_utils: ^1.5.1
  app_links: ^3.5.1
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  email_validator: ^2.1.17

flutter:
  uses-material-design: true
  assets:
    - assets/Icons/
    - .env

version: 1.0.0+1

dev_dependencies:
  flutter_launcher_icons: ^0.13.1